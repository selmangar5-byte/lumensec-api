<div class="p-10 border-t-8 border-indigo-600">
  <div class="flex justify-between items-start mb-12">
    <div>
      <h1 class="text-4xl font-black uppercase text-indigo-600">Lumensec SOC</h1>
      <p class="text-xs font-mono text-slate-500 mt-1">INCIDENT EVIDENCE REPORT</p>
    </div>
    <div class="text-right">
      <p class="text-[10px] font-mono text-slate-400 uppercase"><%= Time.current.strftime("%d/%m/%Y - %H:%M") %></p>
    </div>
  </div>

  <div class="bg-slate-50 p-6 rounded-2xl mb-10 border border-slate-200">
    <h2 class="text-sm font-black uppercase mb-4 text-slate-700">Résumé de l'Incident</h2>
    <div class="grid grid-cols-2 gap-8">
      <div>
        <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Trace ID</p>
        <p class="text-xs font-mono font-bold"><%= incident.id %></p>
      </div>
      <div>
        <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Sévérité / Statut</p>
        <p class="text-xs font-bold uppercase">
          <span class="<%= incident.severity >= 4 ? 'text-red-600' : 'text-indigo-600' %>">Niveau <%= incident.severity %> / 5</span>
          <span class="mx-2 text-slate-300">|</span>
          <span class="text-slate-600"><%= incident.status.humanize.upcase %></span>
        </p>
      </div>
      <div class="col-span-2">
        <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Résumé</p>
        <p class="text-sm leading-relaxed"><%= incident.narrative["summary"] %></p>
      </div>
    </div>
  </div>

  <div class="mb-10">
    <h2 class="text-sm font-black uppercase mb-4 text-slate-700">Artefacts</h2>
    <table class="w-full text-left border-collapse border border-slate-200">
      <thead>
        <tr class="bg-slate-100">
          <th class="p-3 text-[10px] font-black uppercase text-slate-500 border border-slate-200">Type</th>
          <th class="p-3 text-[10px] font-black uppercase text-slate-500 border border-slate-200">Label</th>
          <th class="p-3 text-[10px] font-black uppercase text-slate-500 border border-slate-200">Valeur</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-200">
        <% pack.data["items"].each do |item| %>
          <tr>
            <td class="p-3 border border-slate-200"><span class="px-2 py-1 bg-indigo-50 text-indigo-600 text-[9px] font-bold rounded uppercase"><%= item["type"] %></span></td>
            <td class="p-3 border border-slate-200 text-xs font-bold"><%= item["label"] %></td>
            <td class="p-3 border border-slate-200 text-[10px] font-mono text-slate-600"><%= item["value"] %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="mt-16 pt-8 border-t border-slate-200">
    <p class="text-[9px] text-slate-400 uppercase tracking-widest">Lumensec SOC — Evidence Report</p>
    <p class="text-[8px] text-slate-300 font-mono mt-1">
      [DEMO] Audit ID : <%= SecureRandom.hex(6).upcase %> — Cet identifiant n'est pas lié à un registre d'audit. Ne pas utiliser comme preuve.
    </p>
  </div>
</div>