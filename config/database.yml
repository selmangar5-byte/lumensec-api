default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres

development:
  <<: *default
  database: lumensec_development

test:
  <<: *default
  database: lumensec_test

production:
  <<: *default
  database: lumensec_production
